[phases.setup]
nixPkgs = ["python311", "chromium", "nss", "nspr", "atk", "cups", "libdrm", "dbus", "expat", "libxkbcommon"]

[phases.build]
cmds = ["pip install -r requirements.txt", "playwright install chromium"]
